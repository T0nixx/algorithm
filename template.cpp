#include <iostream>
#include <vector>

using namespace std;

int main() {
    int current_case = 0;
    int case_count;
    cin >> case_count;
    vector<int> outputs;

    while (current_case++ < case_count) {
        int N;
        cin >> N;
    }
    for (int output : outputs) cout << output << '\n';
    return 0;
}
